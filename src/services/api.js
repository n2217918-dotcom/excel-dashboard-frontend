// Single place holding the backend's base URL. Both auth.js and
// Dashboard.jsx import this, instead of each hardcoding the same URL
// separately - if the backend ever moves, only this line changes.

export const API_BASE_URL = "https://excel-dashboard-backend-q2nl.onrender.com";
